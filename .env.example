# Example .env file for custom-resumes
# Copy this to .env and fill in your values

# OpenAI API key (required if using OpenAI)
OPENAI_API_KEY=sk-...

# AI provider (default: openai)
AI_PROVIDER=openai

# Optional: set default model (overridden by --model argument)
# OPENAI_MODEL=gpt-4o
